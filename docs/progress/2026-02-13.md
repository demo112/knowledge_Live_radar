# 2026-02-13 进度日志

## 核心管理增强 (Core Management Enhancement)

### ✅ 已完成
- **数据模型**: 扩展 `PyramidNode`，新增 `NodeRelation` 模型 (支持跨金字塔关联)。
- **核心服务**:
  - 实现节点拆分 (`split_node`) 和合并 (`merge_nodes`) 逻辑。
  - 实现节点关联 (`link_nodes`) 逻辑。
  - 实现多维度健康度评估 (`HealthEvaluator`: 深度平衡、覆盖度、活跃度)。
  - 实现可视化数据服务 (`VisualizationService`: ReactFlow 格式)。
- **API 接口**:
  - `POST /nodes/{id}/split`
  - `POST /pyramids/{id}/merge`
  - `POST /nodes/{id}/link`
  - `GET /pyramids/{id}/health`
  - `GET /pyramids/{id}/visualization`
- **验证**:
  - 编写集成测试 `backend/tests/integration/test_pyramid_operations.py`。
  - 执行并通过所有测试用例。

### 📝 文档更新
- 更新 `changelog.md`。
- 更新 `api-contract.md`。
- 更新 `features/core-management-enhancement/tasks.md`。

### ⏭️ 下一步
- 前端集成 (ReactFlow 可视化组件开发)。

## 扩展信息源类型 (Extended Information Sources)

### ✅ 已完成
- **Schema**: 更新 `SourceCreate` 支持 `WECHAT_MP`, `BILIBILI_USER` 等类型。
- **Fetcher**: 实现 `RSSHubFetcher`，支持将新类型自动转换为 RSSHub 订阅 URL。
- **API**: 更新 Fetcher 工厂，自动路由新类型到 `RSSHubFetcher`。
- **UI**: 更新信息源管理页，支持新类型选择和动态输入提示。
- **验证**:
  - 单元测试: `backend/tests/unit/test_rsshub_fetcher.py` (URL 转换逻辑)
  - 集成测试: `backend/tests/integration/test_crawl_rsshub.py` (Mock 抓取流程)

### 📝 文档更新
- 创建 `features/extended-info-sources/requirements.md`
- 创建 `features/extended-info-sources/tasks.md`
- 更新 `changelog.md`
- 更新 `api-contract.md`

## AI 核心能力 (AI Core Capabilities)

### ✅ 已完成
- **基础设施**:
  - 创建 Prompt 模板 (`backend/app/prompts/*.yaml`)。
  - 实现 `PromptLoader` 自动同步模板到数据库。
  - 扩展 `ContentItem` 模型，新增 `summary`, `tags`, `concepts`, `ai_processed` 字段。
- **核心服务**:
  - 增强 `AIService` 支持结构化 JSON 输出和多任务（摘要、标签、概念）。
  - 重构 `SoftValidator` 使用 AI 进行多维度质量评分。
  - 升级 `ContentProcessor` 集成完整的 AI 处理流水线。
- **前端展示**:
  - 更新 `ContentItem` 和 `Approval` 类型定义。
  - 升级 `FeedPage` 展示 AI 摘要、标签和概念预览。
  - 优化 `ApprovalList` 展示 AI 置信度。
- **验证**:
  - 编写手动集成测试 `backend/tests/integration/test_ai_flow_manual.py`。
  - 验证从抓取到 AI 处理再到数据库存储的完整链路。

### 📝 文档更新
- 创建 `features/ai-capabilities/requirements.md`
- 创建 `features/ai-capabilities/design.md`
- 创建 `features/ai-capabilities/tasks.md`
- 更新 `changelog.md`

### ⏭️ 下一步
- 实现基于 AI 概念提取的金字塔结构进化建议。

## 金字塔自动进化 (Pyramid Evolution)

### ✅ 已完成
- **基础设施**: 集成 ChromaDB 和 SentenceTransformers，实现 VectorService。
- **核心逻辑**: 实现 EvolutionEngine，支持内容自动归类和聚类发现。
- **节点服务**: 扩展 NodeService 支持内容关联 (`link_content`) 和统计更新。
- **前端集成**:
  - 实现 `NodeContents` 组件展示关联内容。
  - 节点详情页和内容详情页增加 AI 归类交互。
- **验证**:
  - 增加 VectorService Dummy 模式以支持无模型测试。
  - 完成 E2E 测试 `evolution.spec.ts` 覆盖全流程。

### 📝 文档更新
- 更新 `features/pyramid-evolution/tasks.md`
- 更新 `changelog.md`

### ⏭️ 下一步
- 准备发布 Release v1.0。
