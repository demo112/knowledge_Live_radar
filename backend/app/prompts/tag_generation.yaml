scene: tag_generation
name: 自动标签生成
description: 为内容自动生成分类标签。
version: "1.0.0"
input_variables:
  - content
template_content: |
  请为以下内容生成 1-3 个分类标签。
  标签应简短有力，例如：React, Python, 机器学习, 架构设计。

  内容:
  {content}

  请以 JSON 格式返回字符串列表：
  ["标签1", "标签2"]
