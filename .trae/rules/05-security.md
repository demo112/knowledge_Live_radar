# 安全规范

| 规则 | 说明 |
|------|------|
| 禁止硬编码密钥 | 使用环境变量 |
| 输入验证 | 所有用户输入必须验证 |
| SQL注入防护 | 使用Prisma参数化查询 |
| XSS防护 | 输出转义 |
| CSRF防护 | 使用Token |
| 敏感操作日志 | 登录、权限变更必须记录 |
| 密码存储 | bcrypt加密，禁止明文 |
| Token | JWT，设置过期时间 |
